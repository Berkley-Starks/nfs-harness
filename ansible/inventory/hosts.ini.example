# Generated by bin/harness from `terraform output`. Do not hand-edit; this
# example just shows the shape. Real file is inventory/hosts.ini (gitignored).

[clients]
client-0 ansible_host=203.0.113.10
client-1 ansible_host=203.0.113.11
client-2 ansible_host=203.0.113.12

# Present only when nfs_backend = self_managed (EFS has no host to configure).
[nfs_server]
nfs-server ansible_host=203.0.113.20

[all:vars]
ansible_user=ec2-user
